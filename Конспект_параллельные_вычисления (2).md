# Подготовка к экзамену по параллельным вычислениям

Конспект составлен по трём пособиям Д. Л. Головашкина:
- **[М]** — «Модели в теории параллельных вычислений» (Самара, 2019);
- **[В]** — «Векторные алгоритмы вычислительной линейной алгебры»;
- **[П]** — «Параллельные алгоритмы вычислительной линейной алгебры» (добавлено позже; закрывает вопросы 23–30 и 36–38).

Формат каждого ответа: краткий тезис → развёрнутое объяснение с примерами → формулы/схемы/псевдокод.

> **Важно об источниках.** Все вопросы покрыты пособиями лектора: 1, 3–22 — по [М] и [В]; 23–30 и 36–38 — по [П] «Параллельные алгоритмы вычислительной линейной алгебры». Везде, где определения/нотация лектора расходятся с общей литературой (масштабируемость, прогонка, pivoting в LU), на экзамене следует опираться на [П].

---

# Содержание

**БЫСТРЫЕ АЛГОРИТМЫ**

- [1. Умножение матриц по Винограду](#1-умножение-матриц-по-винограду)

**МОДЕЛИ В ТЕОРИИ ПАРАЛЛЕЛЬНЫХ ВЫЧИСЛЕНИЙ**

- [3. Классификация вычислительных систем по Флинну](#3-классификация-вычислительных-систем-по-флинну)
- [4. Классификация вычислительных систем по Хокни](#4-классификация-вычислительных-систем-по-хокни)
- [5. Модели представления параллельных алгоритмов](#5-модели-представления-параллельных-алгоритмов)
- [6. Сети как модели вычислительных процессов](#6-сети-как-модели-вычислительных-процессов)
- [7. Сети Петри как модели вычислительных процессов](#7-сети-петри-как-модели-вычислительных-процессов)

**АВТОМАТИЧЕСКОЕ РАСПАРАЛЛЕЛИВАНИЕ**

- [9. Ациклические фрагменты: стандартный граф и граф зависимостей](#9-ациклические-фрагменты-стандартный-граф-и-граф-зависимостей)
- [10. Ациклические фрагменты: построение ЯПФ и параллельного алгоритма](#10-ациклические-фрагменты-построение-япф-и-параллельного-алгоритма)
- [11. Ациклические фрагменты: формализм определения функции «c»](#11-ациклические-фрагменты-формализм-определения-функции-c)
- [12. Циклические фрагменты: пространство итераций, ускорение, метод пирамид](#12-циклические-фрагменты-пространство-итераций-ускорение-метод-пирамид)
- [13. Циклические фрагменты: пространство итераций, ускорение, метод параллелепипедов](#13-циклические-фрагменты-пространство-итераций-ускорение-метод-параллелепипедов)
- [14. Синтетический алгоритм, объединяющий методы параллелепипедов и пирамид](#14-синтетический-алгоритм-объединяющий-методы-параллелепипедов-и-пирамид)

**ВЕКТОРНЫЕ АЛГОРИТМЫ**

- [15. Основные операции с векторами и матрицами. Векторные алгоритмы умножения матриц](#15-основные-операции-с-векторами-и-матрицами-векторные-алгоритмы-умножения-матриц)
- [16. Векторные алгоритмы решения СЛАУ с треугольной матрицей](#16-векторные-алгоритмы-решения-слау-с-треугольной-матрицей)
- [17. Векторные алгоритмы LU-разложения через модификацию матрицы внешним произведением](#17-векторные-алгоритмы-lu-разложения-через-модификацию-матрицы-внешним-произведением)
- [18. Векторные алгоритмы LU-разложения через gaxpy](#18-векторные-алгоритмы-lu-разложения-через-gaxpy)
- [19. Векторные алгоритмы разложения Холецкого через модификацию матрицы внешним произведением](#19-векторные-алгоритмы-разложения-холецкого-через-модификацию-матрицы-внешним-произведением)
- [20. Векторные алгоритмы разложения Холецкого через gaxpy](#20-векторные-алгоритмы-разложения-холецкого-через-gaxpy)

**ПАРАЛЛЕЛЬНЫЕ АЛГОРИТМЫ**

- [21. Рассылка данных по бинарному дереву](#21-рассылка-данных-по-бинарному-дереву)
- [22. Параллельная реализация рекуррентных выражений](#22-параллельная-реализация-рекуррентных-выражений)
- [23. Систолический gaxpy на кольце, декомпозиция матрицы на блочные строки](#23-систолический-gaxpy-на-кольце-декомпозиция-матрицы-на-блочные-строки)
- [24. Систолический gaxpy на кольце, декомпозиция матрицы на блочные столбцы](#24-систолический-gaxpy-на-кольце-декомпозиция-матрицы-на-блочные-столбцы)
- [25. Столбцово-ориентированные алгоритмы умножения матриц на кольце](#25-столбцово-ориентированные-алгоритмы-умножения-матриц-на-кольце)
- [26. Строчно-ориентированные алгоритмы умножения матриц на кольце](#26-строчно-ориентированные-алгоритмы-умножения-матриц-на-кольце)
- [27. Систолический алгоритм умножения матриц на торе](#27-систолический-алгоритм-умножения-матриц-на-торе)
- [28. Параллельный алгоритм разложения Холецкого на кольце](#28-параллельный-алгоритм-разложения-холецкого-на-кольце)
- [29. Параллельный алгоритм LU-разложения на кольце](#29-параллельный-алгоритм-lu-разложения-на-кольце)
- [30. Параллельный алгоритм метода прогонки](#30-параллельный-алгоритм-метода-прогонки)

**ХАРАКТЕРИСТИКИ ПАРАЛЛЕЛЬНЫХ АЛГОРИТМОВ**

- [36. Ускорение, эффективность, масштабируемость, закон Амдала](#36-ускорение-эффективность-масштабируемость-закон-амдала)
- [37. Учёт коммуникаций при оценке эффективности. Коэффициент сетевой деградации](#37-учёт-коммуникаций-при-оценке-эффективности-коэффициент-сетевой-деградации)
- [38. Правила постановки вычислительных экспериментов и анализа их результатов](#38-правила-постановки-вычислительных-экспериментов-и-анализа-их-результатов)

**РЕЗЮМЕ ПО ТЕМЕ**


---

# БЫСТРЫЕ АЛГОРИТМЫ

## 1. Умножение матриц по Винограду

**Тезис.** Метод Винограда (Ш. Виноград, нач. 1960-х) перестраивает скалярное произведение строки на столбец так, что число *умножений* сокращается вдвое за счёт примерно полуторакратного роста числа *сложений*; часть слагаемых вычисляется заранее для каждой строки A и столбца B [В, §1.3].

**Идея.** Классически `c(i,j) = Σ_k a(i,k)·b(k,j)`. Виноград предложил группировать соседние слагаемые попарно и пользоваться тождеством `xy = (x+w)(y+z) − xz − wy` так, что для квадратных матриц получается формула (q — общая размерность суммирования, q чётное):

```
c(i,j) = Σ_{k=1..q/2} ( a(i,2k-1)+b(2k,j) )·( a(i,2k)+b(2k-1,j) )
         − Σ_{k=1..q/2} a(i,2k-1)·a(i,2k)            ← row(i)
         − Σ_{k=1..q/2} b(2k-1,j)·b(2k,j)            ← col(j)
```

**Ключ к экономии.** Вторая сумма зависит только от строки i матрицы A, третья — только от столбца j матрицы B. Их считают **на подготовительном этапе** один раз: `row(i)` используется m раз (для всей i-й строки C), `col(j)` — n раз. Поэтому алгоритм распадается на два подготовительных и один основной.

**Псевдокод (скалярный вид, [В] Алг. 16–18):**

```
# подготовка A
for i=1:n
   row(i)=A(i,1)·A(i,2)
   for k=2:q/2  row(i)=row(i)+A(i,2k-1)·A(i,2k)  end
end
# подготовка B
for j=1:m
   col(j)=B(1,j)·B(2,j)
   for k=2:q/2  col(j)=col(j)+B(2k-1,j)·B(2k,j)  end
end
# основной этап
for i=1:n
  for j=1:m
     C(i,j) = -row(i) - col(j)
     for k=1:q/2
        C(i,j)=C(i,j)+(A(i,2k-1)+B(2k,j))·(A(i,2k)+B(2k-1,j))
     end
  end
end
```

**Подсчёт умножений:** `n·q/2 + m·q/2 + n·m·q/2 = q(n/2+m/2+n·m/2)` против `q·n·m` классического метода. Для квадратных матриц, отбрасывая младшие степени, — **двукратный выигрыш по умножениям**.

**Векторизация [В].** «Свёртывание» внутреннего цикла даёт варианты:
- через **скалярное произведение** чётных/нечётных подвекторов строк A и столбцов B (Алг. 19–21) — но со смешанным доступом к памяти;
- через **покомпонентное произведение** (Алг. 22–25, ikj- и jki-варианты) — с единым доступом по строкам или столбцам, что для памяти выгоднее.

**Замечание о месте в иерархии «быстрых» методов:** Виноград экономит лишь умножения (общая арифметика даже чуть растёт); качественный прорыв дал Штрассен — O(n^2.807), а Копперсмит–Виноград — O(n^2.376), но последний имеет лишь теоретическую ценность (гигантская константа).

---

# МОДЕЛИ В ТЕОРИИ ПАРАЛЛЕЛЬНЫХ ВЫЧИСЛЕНИЙ

## 3. Классификация вычислительных систем по Флинну

**Тезис.** М. Флинн (1966) классифицирует ЭВМ как «чёрный ящик» по двум признакам — множественности **потока команд** и **потока данных**, получая четыре класса [М, §1.1.1].

**Четыре модели (таблица Флинна):**

| | один поток команд | множество потоков команд |
|---|---|---|
| **один поток данных** | SISD — однопроцессорная | MISD — конвейерная |
| **множество потоков данных** | SIMD — векторная | MIMD — многопроцессорная |

- **SISD** (Single Instruction / Single Data) — классическая однопроцессорная ЭВМ: процессор + память, последовательное исполнение. На неё ориентировано традиционное изложение численных методов.
- **SIMD** (Single Instruction / Multiple Data) — **векторная** ЭВМ: одно устройство управления и множество АЛУ, одна команда применяется к разным участкам данных (элементам векторов/матриц) одновременно. Производительность растёт с числом АЛУ; нужна **векторизация** метода.
- **MISD** (Multiple Instruction / Single Data) — **конвейерная**: разные команды над разными фрагментами одного потока данных, несколько процессоров со своей памятью. Сам Флинн примеров MISD не привёл; важна как модель. Недогруженные ступени конвейера простаивают, если метод не делится на фрагменты равной сложности.
- **MIMD** (Multiple Instruction / Multiple Data) — **многопроцессорная**: множество процессоров над множеством данных.

**Типичная ошибка:** относить конвейер к SISD. По Флинну конвейер — это MISD (несколько потоков команд над одним потоком данных).

## 4. Классификация вычислительных систем по Хокни

**Тезис.** Р. Хокни детализирует класс MIMD Флинна, разделяя многопроцессорные ЭВМ по способу соединения процессоров; конвейерные машины Хокни относит к многопроцессорным, оставляя класс MISD пустым [М, §1.1.2].

**Дерево классификации MIMD по Хокни:**

```
MIMD
├── переключаемые (все со всеми через переключатель)
│     ├── с общей памятью      (коммуникации через общую память; нужна
│     │                          регуляция очерёдности доступа)
│     └── с распределённой     (память локальная; полная связность
│         памятью               технически сложна при больших p)
└── сети (процессор связан с фиксированным числом соседей)
      линия, кольцо, бинарное дерево, решётка, тор, звезда, гиперкуб
```

**Сетевые конфигурации [М, рис. 1.5]:**
- **линия** — все, кроме крайних, имеют двух соседей;
- **кольцо** — линия с замкнутыми концами;
- **решётка** и **тор** — переход к многомерной нумерации (номер = вектор); в торе противоположные края решётки соединены, у каждого узла по 4 соседа;
- **звезда** — центральный узел (manager) и периферийные (workers); недостаток — маршрутизация через единственный узел;
- **бинарное дерево** — удобно для рассылки/сбора по принципу «разделяй и властвуй»: вместо `p−1` коммуникаций звезды получаем `log₂p`;
- **гиперкуб** размерности m: `p = 2^m` узлов, каждый связан с m соседями. Из него игнорированием связей получаются линия, кольцо, дерево, решётка, тор; при p=16 гиперкуб совпадает с тором.

**Связь с Флинном:** классификация Хокни — это уточнение **одного** класса (MIMD) классификации Флинна.

## 5. Модели представления параллельных алгоритмов

**Тезис.** Алгоритм по А. А. Маркову — «точное предписание, определяющее вычислительный процесс, ведущий от варьируемых исходных данных к искомому результату» (свойства: определённость, массовость, результативность). Для параллельных вычислений используют две дополняющие друг друга модели: **задача/канал** и **нотацию Дж. Голуба** [М, §1.2].

**Модель «задача/канал»** — ориентированный граф:
- **задачи** (вершины-окружности) содержат инструкции и данные; внутри задачи инструкции строго последовательны;
- **каналы** (дуги) — однонаправленные очереди FIFO для пересылки данных; для обратной передачи нужен отдельный канал;
- параллельность = одновременное исполнение инструкций в разных задачах; задачи могут порождаться и упраздняться.

Коммуникации классифицируют по парам признаков: **локальные/глобальные** (логарифмическая или более сильная зависимость числа соседей от p; линия, кольцо, дерево, тор, гиперкуб — локальные; полносвязная и звезда — глобальные), **синхронные/асинхронные**, **статические/динамические**, **регулярные/нерегулярные**.

**Нотация Дж. Голуба** конкретизирует содержание задач (синтаксис, близкий к MATLAB). Один набор инструкций пишется для всех задач; в зависимости от номера задачи µ часть инструкций исполняется или пропускается. Состоит из:
- **инициализации**: p (число задач), µ (номер задачи), распределение данных, соседи µ;
- **тела алгоритма**.

Соседи: одномерно `left=µ−1, right=µ+1`; двумерно `east, west, north, south`. Коммуникации:
- сеть: `send(что, кому)`, `recv(что[, откуда])`;
- общая память: `put(локальное, общее)`, `get(общее, локальное)`, плюс `begin/end critical section`, `barrier`, индекс `loc`.

**Распределение данных:** линейное `a((µ−1)r+1 : µr)` и циклическое `a(µ:p:n)`, r=n/p. Для матриц — с учётом строчного/столбцового хранения (табл. 1.2 в [М]). Важное понятие — **шаг выборки**; предпочтителен единичный шаг (лучше конвейеризация и доступ к памяти).

## 6. Сети как модели вычислительных процессов

**Тезис.** Прежде чем ввести сети Петри, [М, §1.3.3.1] определяет **сеть** N как тройку `N = (P, T, F)` — ориентированный граф с двумя типами вершин (без разметки, т.е. без «динамики»).

**Определение.** `N = (P, T, F)`, где
- **P** — непустое конечное множество **мест** (окружности); связаны с *условиями*;
- **T** — непустое конечное множество **переходов** (отрезки); связаны с *действиями*;
- **F** — **функция инцидентности** (дуги) с областью определения `P×T ∪ T×P` и значениями в множестве натуральных чисел с нулём (кратность дуги).

Таким образом, сеть — двудольный ориентированный граф: дуги соединяют только вершины разных типов.

**Свойства сети [М]:**
1. `P ∩ T = ∅` — места и переходы не пересекаются.
2. `∀p∈P ∃t∈T: F(p,t)≠0 или F(t,p)≠0` — каждое место связано хотя бы с одним переходом.
3. `∀t∈T ∃p∈P: F(t,p)≠0 или F(p,t)≠0` — каждый переход связан хотя бы с одним местом.
4. `∀p₁,p₂∈P: (•p₁=•p₂) и (p₁•=p₂•) ⟹ p₁=p₂` — не бывает двух разных мест с одинаковым набором входных и выходных переходов. (Для переходов аналогичное свойство **не** требуется.)

Обозначения: `•p` (`p•`) — множество входных (выходных) переходов места p; аналогично `•t`, `t•` — входные/выходные места перехода.

**Связь с другими моделями процессов** (§1.3): пространственно-временные диаграммы и графы следования описывают **уже состоявшийся** процесс, тогда как сети (и сети Петри) позволяют его **моделировать**.

## 7. Сети Петри как модели вычислительных процессов

**Тезис.** Сеть Петри — это сеть, дополненная **разметкой**: `PN = (P, T, F, M)`. Разметка «оживляет» сеть: переходы срабатывают, разметка меняется, моделируя протекание процесса [М, §1.3.3].

**Определения.**
- **Разметка** M — функция `P → {0,1,2,…}`; графически — фишки в местах; векторная форма `M=(M(p₁),…,M(pₖ))`.
- **Условие срабатывания** перехода t: `∀p∈•t  M(p) ≥ F(p,t)`, в векторной форме `M ≥ •F(t)`. Графически — фишек в каждом входном месте не меньше, чем дуг, ведущих в переход. Переход **может** (но не обязан) сработать.
- **Правило смены разметки** при срабатывании t (M → M′):
  ```
  ∀p ∈ •t ∪ t•   M′(p) = M(p) − F(p,t) + F(t,p)
  ∀p ∉ •t ∪ t•   M′(p) = M(p)
  векторно:      M′ = M − •F(t) + F*(t)
  ```
  То есть из входных мест фишки изымаются (по кратности входных дуг), в выходные — добавляются (по кратности выходных дуг).

**Пример [М, §1.3.3.2].** Алгоритм решения квадратного уравнения моделируется сетью на 6 местах и 5 переходах: последовательность срабатываний t₁→t₂→(t₃,t₄)→t₅ переводит фишку от входного места к выходному, отражая ход вычислений (включая возможность параллельного срабатывания независимых переходов). Сети Петри применяют также для моделирования циклических процессов и классической «задачи о пяти философах» (моделирование тупиков/синхронизации).

**Зачем нужны:** в отличие от графов следования, сеть Петри моделирует динамику — позволяет исследовать достижимость разметок, тупики, параллелизм, безопасность.

---
# АВТОМАТИЧЕСКОЕ РАСПАРАЛЛЕЛИВАНИЕ

> Общая схема (ациклические фрагменты): последовательная программа → **стандартный граф** → **граф зависимостей** → **ярусно-параллельная форма (ЯПФ)** → **параллельный алгоритм** (с функцией синхронизации **c**). Допускаются только два вида операторов: присваивание и условный переход.

## 9. Ациклические фрагменты: стандартный граф и граф зависимостей

**Тезис.** По программе строят **стандартный граф** (порядок следования операторов), а затем **граф зависимостей** (что от чего реально зависит по данным/логике/конкуренции) — он и определяет допустимый параллелизм [М, §2.1.1–2.1.2].

**Стандартный граф.** Две разновидности вершин:
- **преобразователи** (прямоугольники) — операторы присваивания (с одним оператором рядом стоящих может быть связан блок);
- **распознаватели** (окружности) — операторы условного перехода.

Дуги задаются отношением **непосредственного следования** (порядок в тексте + переходы). Из преобразователя выходит ≤1 дуги, из распознавателя ≤2 (маркировка «1» — истина, «0» — ложь). **Входная** вершина — без входящих дуг, **выходная** — без исходящих.

**Граф зависимостей** имеет те же вершины, но другие дуги. Три бинарных отношения (далее берётся их общее транзитивное замыкание):

1. **Информационная зависимость** (A′ зависит от A): A предшествует A′ на одном пути, A изменяет переменную, которую A′ использует, не перезаписывая: `Out(A) ∩ (In(A′)\Out(A′)) ≠ ∅`, и между ними нет вершины, переписывающей эту переменную. (Зависимость «чтение-после-записи», RAW.)
2. **Логическая зависимость** (A′ от A): A — распознаватель, и существует другой путь, совпадающий с первым до A включительно, далее отличающийся и **не содержащий** A′. (Управление: исполнится A′ или нет, зависит от ветвления.)
3. **Конкуренционная зависимость** (A′ от A): обе изменяют одну переменную `Out(A) ∩ Out(A′) ≠ ∅` (только преобразователи; «запись-после-записи», WAW).

Обозначения: `Out(A)` — переменные в левых частях присваиваний A; `In(A′)` — переменные в правых частях (или в условии, если A′ — распознаватель).

**Пример (квадратное уравнение, Программа 2.1).** На стандартном графе C непосредственно следует за B; на графе зависимостей C от B **не** зависит (зато появляется зависимость A→C). После транзитивного замыкания H оказывается зависящей от C (хотя логической зависимости нет: H исполнится при любой ветви).

Запись `dep(A,A′)` — «A′ зависит от A» (по графу зависимостей); о *путях* говорят по стандартному графу.

## 10. Ациклические фрагменты: построение ЯПФ и параллельного алгоритма

**Тезис.** **ЯПФ** распределяет вершины графа зависимостей по ярусам так, что вершины одного яруса независимы и могут исполняться параллельно; затем ярусы циклически распределяются по задачам [М, §2.1.3–2.1.4].

**Построение ЯПФ:**
- **Шаг 1.** В 1-й ярус — вершины, не зависящие ни от какой `∀A′ ¬dep(A′,A)`.
- **Шаг 2.** Если все вершины распределены — конец.
- **Шаг 3.** В ярус k+1 — вершины, зависящие **только** от вершин предыдущих ярусов. Увеличить k, перейти на шаг 2.

Пример (Программа 2.1): ярус1=A; ярус2=B,C; ярус3=D,E,F; ярус4=G; ярус5=H. Вершины одного яруса (B и C; затем D,E,F) исполнимы параллельно.

**Синтез параллельного алгоритма** (p задач):
- **Шаг 1.** В задачу µ из 1-го яруса — вершины `A_{1,µ}, A_{1,µ+p}, A_{1,µ+2p}, …` (**циклическое** распределение).
- **Шаг 3.** Для очередного яруса q к задаче µ относят `c(A_{q,µ}); A_{q,µ}; c(A_{q,µ+p}); A_{q,µ+p}; …` — каждой вершине, кроме первого яруса, предшествует **функция c** (синхронизация/приём данных).

Несбалансированность (мало вершин на ярусе при большом p) лечат не изменением правила, а уменьшением числа задач.

**Пример (p=2):**

| ярус | задача 1 | задача 2 |
|---|---|---|
| 1 | A | |
| 2 | c(C); C | c(B); B |
| 3 | c(D); D; c(E); E | c(F); F |
| 4 | c(G); G | |
| 5 | c(H); H | |

## 11. Ациклические фрагменты: формализм определения функции «c»

**Тезис.** Функция `c(A)` управляет исполнением вершины A в параллельном алгоритме: разрешает/запрещает исполнение или приостанавливает процесс до прихода данных. На практике `c(A)` — это инструкции **приёма** данных, от которых зависит A [М, §2.1.4].

**Три значения `c(A)`:**
- **1 («истина»)** — операторы A подлежат исполнению;
- **0 («ложь»)** — операторы A пропускаются, процесс переходит к следующей `c(..)`;
- **не определено** — процесс приостанавливается до определения значения (ожидание данных).

(Строго говоря, `c` — не функция в математическом смысле, т.к. учитывает развитие процесса во времени.)

**Формализм.** Пусть из входной вершины стандартного графа в A ведёт **S путей**. С каждым путём s свяжем `c_s`:
- `c_s = 1`, если на пути s исполнились все вершины, от которых A зависит, удовлетворяя зависимости;
- `c_s = 0`, если хотя бы один такой оператор заведомо не исполнится нужным образом;
- не определено, если ход процесса ещё не ясен.

Тогда
```
c(A) = c_1 ∨ c_2 ∨ … ∨ c_S          (дизъюнкция по путям)
```
Для каждого пути s, выписав зависимые от A вершины в порядке следования `A_1,…,A_I`:
```
c_s = c_1 ∧ c_2 ∧ … ∧ c_I           (конъюнкция вдоль пути)
```
где при 1≤i≤I:
- `c_i = c(A_i)`, если `A_i` — преобразователь;
- `c_i = c(A_i=1)`, если `A_i` — распознаватель и для пути s нужна истинность условия (тогда `c_i=1`, иначе 0);
- `c_i = c(A_i=0)`, если для пути нужна ложность условия.

Итог — **дизъюнкция конъюнкций**, часто громоздкая, но упрощаемая:
- не нужно проверять `c(A_i)`, если `A_i` в той же задаче и работала над той же памятью, что A;
- если `c(A_i)` уже проверялась перед другой вершиной той же задачи — достаточно проверить только последнюю.

**Удовлетворение зависимости:** для присваивания — факт исполнения; для условного перехода — исполнение **и** такое значение условия, при котором процесс пошёл по пути, содержащему A.

**Пример [М]:** `c(B)=c(A)` (приём a,b,c); `c(C)=c(A)=1` (та же задача, та же память — без синхронизации); `c(D)=c(C=1)`; `c(E)=c(B)∧c(C=0)`; `c(F)=c(C=0)`; `c(G)=c(C=0)∧c(F)`; `c(H)=c(D)∨c(G)=1`. Реализуется приёмами/отправками `recv/send` и помещением операторов в нужные ветви условного перехода (Алгоритм 2.1).

## 12. Циклические фрагменты: пространство итераций, ускорение, метод пирамид

**Тезис.** Вложенному циклу сопоставляют **пространство итераций** `I={(i₁,…,i_K): 1≤i_k≤n_k}`; цель — найти покрытие `{I_q}` из подмножеств **независимых** векторов (исполняемых параллельно). **Метод пирамид** строит покрытие «от результирующих векторов вниз», исключая коммуникации [М, §2.2.1, §2.2.3].

**Пространство итераций.** Каждой итерации `(i₁,…,i_K)` соответствует вектор. На «развёрнутой» (ациклической) конструкции вводятся отношения **следования** (по порядку исполнения) и **зависимости** (по графу зависимостей). Ограничения метода: нельзя менять параметры цикла в теле и иметь условный переход за тело цикла.

**Ускорение.** Если покрытие состоит из Q подмножеств (каждое — независимые векторы, исполняются за «одну» по длительности),
```
S = (∏_{k=1..K} n_k) / Q
```
— отношение общего числа итераций к числу подмножеств. Лучшим считают покрытие с максимальным S (без учёта коммуникаций; окончательный критерий — эксперимент).

**Метод пирамид (этапы):**
1. Построить пространство итераций с отношениями зависимости и следования (декартово представление желательно, но не обязательно — допускаются **нелинейные** выражения параметров в теле, что недоступно методу параллелепипедов).
2. Выделить **результирующие** векторы — те, от которых не зависит ни один другой. Каждому — своя задача.
3. К каждой задаче добавить **все** векторы, от которых зависит её результирующий; упорядочить по отношению следования. Алгоритм построен.

**Свойства:** покрытие **единственно** (нет оптимизации) и, как правило, **не разбиение** — часть векторов **дублируется** в разных задачах (повтор вычислений — недостаток). **Главное достоинство — отсутствие коммуникаций между задачами**: последний вектор задачи (результирующий) зависит от всех предыдущих в ней, а от векторов чужих задач не зависит. Применим, когда сеть медленная/ненадёжная.

**Пример (трёхслойная разностная схема, Программа 2.5):**
```
for i1=1:n1
  for i2 = max{1, μ-(n1-i1)·tg α} : min{n2, μ+(n1-i1)·tg β}
     x(i1,i2)=f(x(i1-1,i2),x(i1-1,i2+1),x(i1-2,i2),x(i1-2,i2-1))
  end
end
```
Результирующие — последний слой по i1; задача µ «накрывает» треугольник (в 3D — пирамиду), ограниченный прямыми с углами α, β (заданы шаблоном: tg α=½, tg β=1). `max/min` не дают выйти за пространство. Применять при `n2 ≫ n1` (иначе дублирование чрезмерно).

**Модифицированный метод пирамид (укрупнение задач, по Фостеру):** вводят параметр **высота пирамиды** h. Задачи объединяют, разрешая коммуникации через каждые h итераций внешнего цикла. При h=n1 — коммуникаций внутри цикла нет, дублирование максимально; при h=1 — дублирования нет, но коммуникация на каждой итерации. Топология коммуникаций — линия. h подбирают экспериментально.

## 13. Циклические фрагменты: пространство итераций, ускорение, метод параллелепипедов

**Тезис.** **Метод параллелепипедов** ищет покрытие пространства итераций подмножествами-параллелепипедами (отрезки в 1D, параллелограммы в 2D, параллелепипеды в 3D), внутри которых все векторы независимы; в общем случае требует синхронизации, зато допускает **оптимизацию** (несколько покрытий → лучшее по S) [М, §2.2.2].

**Этапы:**
1. Построить декартово пространство итераций с зависимостями и следованием.
2. Найти разбиения на параллелепипеды (размерности ≤ K) из независимых векторов. Этапы 3–4 — для каждого разбиения.
3. Выбрать параллелепипед максимального объёма со сторонами `p₁,…,p_K` (декартова длина +1). Каждому вектору i внутри ставится задача `(g₁,…,g_K)` — координаты в новой системе на рёбрах параллелепипеда, начиная с (1,…,1). Фиктивные стороны имеют длину 1. Число задач `= ∏ p_k`.
4. Распределить **все** векторы по задачам: из каждого параллелепипеда — не более одного вектора в задачу; внутри задачи — по отношению следования; оговорить синхронизацию (через `recv/send`). Общая форма — Алгоритм 2.4.
5. Сравнить алгоритмы, выбрать лучший по S.

**Общая форма параллельного алгоритма (Алг. 2.4):**
```
for i1=g1:p1:n1
  ...
    for iK=gK:pK:nK
       T(i1,...,iK)
       <синхронизация>
    end
  ...
end
```

**Пример (Программа 2.4, `x(i1,i2)=f(x(i1-2,i2-1))`, I: 1≤i1≤7, 1≤i2≤3):** три разбиения дают разные алгоритмы:
- разбиение «а» (пары по i1): 2 задачи, **без** синхронизации, `S=21/12≈1.75`;
- разбиение «б» (полосы по i1): 7 задач, **с** синхронизацией (топология — две линии), `S=21/3=7`;
- разбиение «в» (прямоугольники 2×3): 6 задач, `S=21/4=5.25`.

Видно, что выбор покрытия меняет и ускорение, и наличие коммуникаций — отсюда возможность оптимизации. Условные переходы для синхронизации (`if (i1-2)≥1 … recv …; … if (i1+2)≤7 … send …`) портят производительность, но сохраняют единую форму записи.

**Параллелепипеды vs пирамиды:**

| | параллелепипеды | пирамиды |
|---|---|---|
| покрытие | разбиение, **много** вариантов → оптимизация | единственное, обычно **не** разбиение |
| дублирование векторов | нет | есть (повтор вычислений) |
| коммуникации | как правило нужны | **отсутствуют** (заложено в построение) |
| нелинейные параметры в теле | нельзя | можно |
| простые циклы | можно | нельзя |

## 14. Синтетический алгоритм, объединяющий методы параллелепипедов и пирамид

**Тезис.** Идея синтеза — взять **бескоммуникационность** метода пирамид и **управляемость/балансировку** метода параллелепипедов: пространство итераций делят на крупные блоки (как в параллелепипедах), а внутри блока каждой задаче назначают «пирамиду» зависимых векторов; коммуникации между блоками допускаются дозированно — через **высоту пирамиды** h.

> ⚠ Отдельного раздела «синтетический алгоритм» в пособии [М] нет; ближайший к нему материал — **модифицированный метод пирамид с укрупнением задач** (agglomeration по методике Фостера, [М] §2.2.3, рис. 2.7). Ниже изложено на его основе; если у лектора есть собственная формулировка «синтетического алгоритма», уточните по конспекту лекций.

**Как он работает (по [М, рис. 2.7]):**
1. Пространство итераций делят на Q основных подобластей (это — «параллелепипедная» часть: основные подобласти образуют **разбиение**, обеспечивают балансировку — каждой задаче примерно по `n2/Q` результирующих векторов).
2. Внутри каждой задачи строят пирамиды зависимых векторов (это — «пирамидальная» часть). Чтобы пирамиды соседних задач не приходилось целиком дублировать, у границ оставляют узкие зоны:
   - зона **s** — значения, которые задача **отправляет** соседу;
   - зона **r** — значения, которые задача **принимает** от соседа.
3. Параметр **высота пирамиды h** регулирует компромисс:
   - `h = n1` → коммуникаций внутри цикла нет, но максимальное дублирование (чистый «пирамидальный» предел);
   - `h = 1` → дублирования нет, коммуникация на каждой итерации внешнего цикла (близко к «параллелепипедному» поведению);
   - промежуточные h дают баланс «дублирование ↔ коммуникации».

Топология коммуникаций — **линия**. Оптимальная h подбирается **экспериментально** под конкретную систему (отношение скорости счёта к пропускной способности сети).

**Вывод:** синтетический подход параметрически перекрывает оба крайних метода: варьируя h, переходим от «много дублирования, нет связи» (пирамиды) к «нет дублирования, много связи» (параллелепипеды), выбирая оптимум под архитектуру.

---
# ВЕКТОРНЫЕ АЛГОРИТМЫ

> Сквозная идея пособия [В]: выражать матричные методы через **векторные операции**, аппаратно поддерживаемые SIMD (SSE, GPU). Вычислительная сложность складывается из **арифметической** (флопы) и **коммуникационной** (пересылки между уровнями памяти). Предпочтительны операции с единичным шагом выборки и более «нагруженные» операции (больше арифметики «на месте» → меньше пересылок).

## 15. Основные операции с векторами и матрицами. Векторные алгоритмы умножения матриц

**Тезис.** Операции 1-го уровня (вектор–вектор), 2-го (матрица–вектор) и 3-го (матрица–матрица) различаются по доступу к памяти и длине векторов; умножение матриц записывают в скалярном, векторном и матричном видах, а перестановкой циклов получают 6 алгоритмов с разными свойствами [В, §1.1–1.2].

**Операции 1-го уровня** (n — длина, в скобках число скалярных операций):
- `z=x+y` — сложение (n);
- `z=αx` — умножение на скаляр (n);
- `α=xᵀy` — **скалярное произведение** (2n−1), смешанный доступ;
- `z=x.*y` — покомпонентное умножение (n);
- `z=αx+y` — **saxpy** (scalar alpha x plus y, 2n) — самая удачная: единичный доступ, хорошая векторизация.

**Операции 2-го уровня:**
- `A=A+xyᵀ` — **модификация матрицы внешним произведением** (мм.);
- `z=Ax+y` — **gaxpy** (general A x plus y).

Обе выражаются через saxpy и имеют одинаковую арифметику (2nm флопов), но у **gaxpy вдвое меньше коммуникаций**, чем у мм. (мм. читает и пишет всю матрицу; gaxpy — только читает). Выражение через saxpy:
```
мм. (столбцовое):  for j: A(:,j)=A(:,j)+x·y(j)
gaxpy (столбцовое): for j: y=y+A(:,j)·x(j)
```

**Критерии выбора векторной операции:** единичный шаг выборки (избегать скалярного произведения при одинаковом хранении матриц); максимальная длина векторов (для GPU — «длинные» векторы, «разворачивая» матрицу в один вектор).

**Умножение матриц — три формы (ijk-алгоритм):**
```
скалярный:  for i,j,k: C(i,j)=C(i,j)+A(i,k)B(k,j)
векторный:  for i,j:   C(i,j)=A(i,:)·B(:,j)        # скалярное произв.
матричный:  for i:     C(i,:)=A(i,:)·B             # строчное gaxpy
```

**Классификация (перестановка циклов даёт 6 алгоритмов):** внутренний цикл задаёт **векторную** операцию и доступ, внешний — **матричную**:

| алгоритм | векторная операция | матричная операция | доступ |
|---|---|---|---|
| ijk / jik | скалярное произведение | строчное / столбцовое gaxpy | смешанный |
| jki / ikj | saxpy (столбц./строч.) | столбцовое / строчное gaxpy | по столбцам / строкам |
| kji / kij | saxpy | модификация внешним произв. | по столбцам / строкам |

Лучшие из неблочных: **jki** (хранение по столбцам) и **ikj** (по строкам) — исключают и смешанный доступ, и затратную мм.

**Блочные алгоритмы — самые лучшие.** Матрицы разбивают на блоки α×α (α=n/N); умножение блоков (операция 3-го уровня) даёт **много арифметики на один обмен**: `2k³` флопов на `2k²` данных. Общий объём коммуникаций блочного алгоритма `n²(2N+1)`; при размере «быстрой» памяти M (в элементах) для размещения 3 блоков нужно `3(n/N)²≤M`, откуда **оптимальный блочный размер** `N_opt ≈ n·√(3/M)` (кэш заполнен — обмен минимален). Лучшими признаются варианты с внутренним циклом по kb.

## 16. Векторные алгоритмы решения СЛАУ с треугольной матрицей

**Тезис.** Систему `Lx=b` (L — нижняя треугольная) решают **прямой подстановкой**; в зависимости от доступа к памяти получают вариант на **скалярном произведении** (доступ по строкам) или на **saxpy** (доступ по столбцам) [В, §2.1].

**Формулы прямой подстановки:** `x₁=b₁/l₁₁`, далее `xᵢ=(bᵢ − Σ_{k<i} l_{ik}x_k)/l_{ii}`. Сумма — скалярное произведение `L(i,1:i-1)·x(1:i-1)`.

**Алгоритм на скалярном произведении (доступ по строкам, Алг. 26):**
```
b(1)=b(1)/L(1,1)
for i=2:n
   b(i)=(b(i)-L(i,1:i-1)·b(1:i-1))/L(i,i)   # x пишется поверх b
end
```

**Алгоритм на saxpy (доступ по столбцам, Алг. 27).** Найденный `x_j` сразу подставляется и переносится в правую часть, редуцируя систему; столбец множителей под диагональю даёт saxpy:
```
for j=1:n-1
   b(j)=b(j)/L(j,j)
   b(j+1:n)=b(j+1:n)-L(j+1:n,j)·b(j)        # saxpy
end
b(n)=b(n)/L(n,n)
```

**Системы `LX=B` с q правыми частями** решают как q систем; лучше — **блочный алгоритм** (Алг. 28): диагональные блоки `L_jj` треугольные (решаются малой подсистемой, помещающейся в «быструю» память), внедиагональные — плотные, дают умножение матриц:
```
for j=1:N-1
   решить L_jj·X_j=B_j  (поверх B_j)
   for i=j+1:N  B_i = B_i - L_ij·B_j  end     # умножение матриц
end
решить L_NN·X_N=B_N
```

## 17. Векторные алгоритмы LU-разложения через модификацию матрицы внешним произведением

**Тезис.** `A=LU` (L — нижняя унитреугольная, U — верхняя треугольная) строится методом исключения Гаусса: матрицу A слева последовательно умножают на матрицы преобразования Гаусса `M_k`, обнуляя поддиагональные элементы; каждое умножение `M_k A = A − τ^(k)·A(k,:)` есть **модификация матрицы внешним произведением** [В, §2.2].

**Преобразование Гаусса.** `M_k = E − τ^(k)·e_kᵀ`, где вектор множителей `τ_i^(k)=a_{ik}/a_{kk}` (k+1≤i≤n). Воздействие на матрицу:
```
M_k·A = A − τ^(k)·A(k,:)      # внешнее произведение столбца на строку
```
После `M_{n-1}…M_2 M_1 A = U` под диагональю — нули. Множители удобны тем, что
```
M_k^{-1} = E + τ^(k)·e_kᵀ,   и   L = M_1^{-1}…M_{n-1}^{-1} = E + Σ_{k=1..n-1} τ^(k)·e_kᵀ
```
— то есть L собирается **бесплатно** из уже найденных множителей (записываются под диагональю A).

**Алгоритм (kji, матричный вид, с замещением — Алг. 30):**
```
for k=1:n-1
   A(k+1:n,k)   = A(k+1:n,k)/A(k,k)                        # множители → в L
   A(k+1:n,k+1:n)= A(k+1:n,k+1:n) - A(k+1:n,k)·A(k,k+1:n)  # мм.
end
```
Результат: ниже диагонали — L, на диагонали и выше — U (единичная диагональ L не хранится).

**Характеристики kji-алгоритма:** доступ **по столбцам**; матричная операция — **модификация внешним произведением**; основная векторная — **столбцовое saxpy** (плюс реже — умножение вектора на скаляр для множителей).

## 18. Векторные алгоритмы LU-разложения через gaxpy

**Тезис.** Здесь оставшаяся часть A в вычислениях не участвует: на j-м шаге через **gaxpy** и решение треугольной системы находят сразу j-е столбцы L и U из уже готовых первых j−1 столбцов. Выгода — вдвое меньше коммуникаций, чем у внешнего произведения [В, §2.3].

**Вывод.** Из `A(:,j)=L·U(:,j)`:
- верхняя часть `A(1:j-1,j)=L(1:j-1,1:j-1)·U(1:j-1,j)` — **СЛАУ с унитреугольной L** → решается по Алг. 27 (доступ по столбцам), даёт `U(1:j-1,j)`;
- нижняя часть приводит к вектору `v(j:n)=A(j:n,j) − L(j:n,1:j-1)·U(1:j-1,j)` — это **gaxpy**;
- т.к. `L(j,j)=1`: `U(j,j)=v(j)`, `L(j+1:n,j)=v(j+1:n)/U(j,j)`.

**Алгоритм (jki, с замещением — Алг. 34):**
```
for j=1:n
   for k=1:j-2                                   # решение СЛАУ (13)
      A(k+1:j-1,j)=A(k+1:j-1,j)-A(k+1:j-1,k)·A(k,j)
   end
   A(j:n,j)=A(j:n,j)-A(j:n,1:j-1)·A(1:j-1,j)     # gaxpy
   A(j+1:n,j)=A(j+1:n,j)/A(j,j)                  # j-й столбец L
end
```
Замещение даёт **трёхкратную** экономию памяти (одна матрица вместо трёх).

**Характеристики jki-алгоритма:** доступ по столбцам; матричная операция — **gaxpy**; основная векторная — **столбцовое saxpy** (gaxpy и решение СЛАУ выражаются через него). Отличие от Холецкого — наличие решения треугольной системы.

## 19. Векторные алгоритмы разложения Холецкого через модификацию матрицы внешним произведением

**Тезис.** Для симметричной положительно определённой A разложение `A=GGᵀ` (G — нижняя треугольная) строится по квазиблочной схеме: на каждом шаге находят первый столбец G и **модифицируют** оставшуюся подматрицу внешним произведением [В, §3.2].

**Вывод (квазиблочное представление).** Из
```
[ γ   vᵀ ]   [ √γ      0  ] [ 1            0     ] [ √γ   vᵀ/√γ ]
[ v   B  ] = [ v/√γ  E_{n-1}] [ 0   B − v·vᵀ/γ   ] [ 0    E_{n-1}]
```
подматрица `B − v·vᵀ/γ` снова симметрична и положительно определена → `= G₁G₁ᵀ`. Первый столбец `G = [√γ; v/√γ]`, далее рекурсия по `G₁`. Матричная операция — модификация `B − v·vᵀ/γ` внешним произведением.

**Алгоритм (kji, матричный вид — Алг. 41):**
```
for k=1:n
   A(k:n,k)=A(k:n,k)/√(A(k,k))
   A(k+1:n,k+1:n)=A(k+1:n,k+1:n)-A(k+1:n,k)·[A(k+1:n,k)]ᵀ   # мм.
end
```
G формируется в нижнем треугольнике A. Алгоритм почти идентичен LU через мм. (Алг. 30) и по структуре, и по числу арифметических операций (в мм. задействуются и элементы выше диагонали, хотя G туда не идёт).

**Характеристики kji-алгоритма Холецкого:** доступ по столбцам; матричная операция — модификация внешним произведением; векторная — столбцовое saxpy.

## 20. Векторные алгоритмы разложения Холецкого через gaxpy

**Тезис.** На j-м шаге j-й столбец G находится через **gaxpy** по уже готовым первым j−1 столбцам — как в LU через gaxpy, но **без решения треугольной системы** (отсюда меньшая сложность) [В, §3.1].

**Вывод.** Из `A(:,j)=G·Gᵀ(:,j)`, рассматривая нижнюю часть столбца:
```
A(j:n,j)=G(j:n,1:j-1)·Gᵀ(1:j-1,j) + G(j:n,j)·G(j,j)
```
Вводя `v(j:n)=G(j:n,j)·G(j,j)`:
```
v(j:n)=A(j:n,j) − G(j:n,1:j-1)·Gᵀ(1:j-1,j)     # gaxpy
G(j,j)=√(v(j)),   G(j+1:n,j)=v(j+1:n)/G(j,j)
```
Заметив `Gᵀ(1:j-1,j)=[G(j,1:j-1)]ᵀ` (уже найдено).

**Алгоритм (матричный вид — Алг. 38, с замещением в нижнем треугольнике A):**
```
for j=1:n
   A(j:n,j)=A(j:n,j)-A(j:n,1:j-1)·Aᵀ(1:j-1,j)   # gaxpy, по ф. (22)
   A(j:n,j)=A(j:n,j)/√(A(j,j))                  # ф. (23)
end
```
Для лучшей векторизации деление на диагональный элемент совмещено с вычислением столбца: `G(j,j)=v(j)/√(v(j))=√(v(j))`.

**Сравнение Холецкий vs LU (оба через gaxpy):** структура одинакова, но у Холецкого **нет** решения треугольной системы → ниже вычислительная сложность. **Характеристики jki-алгоритма Холецкого:** доступ по столбцам; матричная операция — столбцовое gaxpy; векторная — столбцовое saxpy.

---
# ПАРАЛЛЕЛЬНЫЕ АЛГОРИТМЫ

> Вопросы 21–22 — по [М]. Вопросы 23–30 — по пособию [П] «Параллельные алгоритмы вычислительной линейной алгебры». Нотация — Голуба: `send/recv`, кольцевые соседи `left/right`, торовые `east/west/north/south`; индекс `loc` — локальные данные задачи; начальное распределение вынесено в «Инициализацию». Систолический алгоритм по [П] — порядок в каждой ветви: `отправка → приём → арифметика`.

## 21. Рассылка данных по бинарному дереву

**Тезис.** Рассылка вектора от задачи 1 ко всем p задачам по бинарному дереву выполняется за `log₂p` параллельных шагов (вместо `p−1` на звезде) по стратегии «разделяй и властвуй»: на каждом уровне владелец данных отдаёт соседу **половину** оставшегося вектора [М, §1.2.3].

**Схема для p=8** (уровни 3→0 сверху вниз):
```
шаг1 (ур.3):  1 → 5 : a(n/2+1 : n)
шаг2 (ур.2):  1 → 3 : a(n/4+1 : n/2);   5 → 7 : a(3n/4+1 : n)
шаг3 (ур.1):  1 → 2;  3 → 4;  5 → 6;  7 → 8   (по «восьмушке»)
шаг4 (ур.0):  чётные задачи принимают
```
Последовательно — всего `log₂8 = 3` коммуникации (без учёта последнего приёма).

**Алгоритм (Голуб, Алг. 1.2 [М]):** задачи делятся на три группы:
```
if µ=1:                                 # первая задача
   k=log2 p
   for i=k:-1:1:  send(a(n/2+1:n), 1+2^(i-1));  n=n/2  end
elif µ чётное:                          # принимают один раз
   recv(a(1:r), µ-1)
else:                                   # нечётные, кроме первой
   # вычислить уровень k приёма (по mod), затем
   n = n / 2^(log2 p - k)
   recv(a(1:n), µ-2^k)
   for i=k:-1:1:  send(a(n/2+1:n), µ+2^(i-1));  n=n/2  end
```
Каждая нечётная задача сначала принимает свой фрагмент с уровня k, затем сама становится «корнем» поддерева и рассылает ниже. Уровень k можно не вычислять, а **передавать** вместе с данными (за последним элементом вектора). Аналогично строится **сбор** данных (рассылка в обратном направлении). В MPI этому соответствует готовая `MPI_Bcast`.

## 22. Параллельная реализация рекуррентных выражений

**Тезис.** Линейная рекурсия `xᵢ=aᵢ·xᵢ₋₁` (нужен xₙ) последовательна по природе (каждый вектор зависит от всех предыдущих), но распараллеливается **сдваиванием** (parallel prefix): попарные произведения за `log₂n` шагов. Рекурсии с добавочными членами сводят к **произведению матриц** [М, §2.2.4].

**Простая рекурсия.** `xₙ=aₙ·aₙ₋₁·…·a₁·x₀`. Задаче µ (1≤µ≤n/2) — произведение `a_{2µ}·a_{2µ-1}`. На 1-м шаге параллельно n/2 умножений, далее n/4, n/8, …, 2 — всего `log₂n − 1` шагов, в конце домножение на x₀.
```
Ускорение S ≈ n / log₂ n
```

**Рекурсия с добавкой** `xᵢ=aᵢ·xᵢ₋₁+bᵢ` → матричная форма. Пусть
```
        [ xᵢ ]            [ aᵢ  bᵢ ]
  xᵢ =  [ 1  ] ,    Aᵢ =  [ 0   1  ]   ⟹   xᵢ = Aᵢ·xᵢ₋₁
```
Тогда `xₙ = Aₙ·Aₙ₋₁·…·A₁·x₀` — произведение матриц распараллеливается сдваиванием. Структура `Aᵢ` (вторая строка `0 1`) при умножении воспроизводится — лишних операций нет.

**Обобщение.** Для `xᵢ=aᵢxᵢ₋₁+bᵢxᵢ₋₂` берут 2×2 матрицу `[[aᵢ,bᵢ],[1,0]]`; для `xᵢ=aᵢxᵢ₋₁+bᵢxᵢ₋₂+cᵢ` — 3×3 и т.д. (вектор и матрица растут с порядком рекуррентности). Идея — превратить рекурсию в **ассоциативную** операцию (умножение матриц), к которой применимо сдваивание.

## 23. Систолический gaxpy на кольце, декомпозиция матрицы на блочные строки

**Тезис.** `z=Ax+y` с распределением матрицы A по **блочным строкам** опирается на **блочное скалярное произведение**: задача µ вычисляет `zµ=Σ_{j=1..p} Aµj·xj + yµ` (ф. 1). Возможны два варианта: без коммуникаций (вектор x дублируется) и систолический на кольце (блоки x циркулируют) [П, §1.1].

Разбиение на блоки: `zµ,yµ∈ℝ^α`, `xj∈ℝ^β`, `Aij∈ℝ^{α×β}`, α=n/p, β=m/p.

**Вариант 1 — без коммуникаций (Алг. 1).** У задачи µ хранится вся блочная строка `Aµ` и **весь** вектор x; коммуникаций нет, плата — дублирование x.
```
Иниц.{row=(µ-1)α+1:µα; Aloc=A(row,:); xloc=x; yloc=y(row)}
for j=1:p
   a=(j-1)β+1:jβ
   yloc = yloc + Aloc(:,a)·xloc(a)     # слагаемое j из ф.(1)
end
```

**Вариант 2 — систолический на кольце (Алг. 2).** У задачи µ — только свой блок `xµ`; блоки x **циркулируют** по кольцу, за p шагов через µ проходят все. Порядок систолический: `send → recv → арифметика`.
```
Иниц.{row=(µ-1)α+1:µα; Aloc=A(row,:); xloc=x((µ-1)β+1:µβ); yloc=y(row); left,right}
for j=1:p
   send(xloc,right);  recv(xloc,left)        # сдвиг блока x по кольцу
   τ=µ-j; if τ≤0 then τ=τ+p end             # индекс принятого блока xτ
   a=(τ-1)β+1:τβ
   yloc = yloc + Aloc(:,a)·xloc             # слагаемое τ из ф.(1)
end
```
**Ключевой приём** — восстановление номера принятого блока `τ=µ−j (+p)`. Недостатки (отмечены в [П]): одновременная отправка во всех задачах перегружает сеть; систоличность мешает чередовать приём/отправку по чётности и вынести последнюю итерацию вперёд.

## 24. Систолический gaxpy на кольце, декомпозиция матрицы на блочные столбцы

**Тезис.** При распределении A по **блочным столбцам** меняется формализм: gaxpy выражается через **линейную комбинацию блочных столбцов** `z=Σ_{j=1..p} Aj·xj + y`. По кольцу циркулируют **частичные векторы** `ωj=Aj·xj` (длины n); задача µ накапливает свой блок `zµ=Σ_j ωj(row)+yµ` (ф. 2) [П, §1.1].

```
Алгоритм 3 (на кольце, блочные столбцы)
Иниц.{row=(µ-1)α+1:µα; col=(µ-1)β+1:µβ; Aloc=A(:,col); xloc=x(col); yloc=y(row); left,right}
ω = Aloc·xloc                       # ωµ
for j=1:p
   send(ω,right);  recv(ω,left)     # частичная сумма идёт по кольцу
   yloc = yloc + ω(row)             # сложение по ф.(2)
end
```

**Сравнение строки/столбцы [П]:**

| | блочные строки (Алг. 2) | блочные столбцы (Алг. 3) |
|---|---|---|
| формализм | блочное скалярное произв. (ф.1) | лин. комбинация столбцов (ф.2) |
| что циркулирует | блоки **x** (β элементов) | векторы **ω** (n элементов) |
| нужен индекс τ | да (`τ=µ−j`) | **нет** (складываем `ω(row)`, коммутативность) |
| объём коммуникаций | меньше | **больше в n/β раз** |
| сложность алгоритма | выше | проще |

То есть столбцовый вариант проще, но дороже по объёму пересылок (вместо коротких xloc гоняются длинные ω).

## 25. Столбцово-ориентированные алгоритмы умножения матриц на кольце

**Тезис.** `D=C+AB` на кольце; распределение **по блочным столбцам**: задача µ хранит столбцы `Bµ,Cµ` и считает блочный столбец `Dµ=Cµ+Σ_{j=1..p} Aj·Bjµ` (ф. 4) — линейная комбинация блочных столбцов A. По кольцу циркулируют **блочные столбцы матрицы A** [П, §1.2, Алг. 5].

```
Алгоритм 5
Иниц.{col=(µ-1)β+1:µβ; col2=(µ-1)γ+1:µγ; Aloc=A(:,col2); Bloc=B(:,col); Cloc=C(:,col); left,right}
for j=1:p
   send(Aloc,right);  recv(Aloc,left)     # столбцы A идут по кольцу
   τ=µ-j; if τ≤0 then τ=τ+p end
   a=(τ-1)γ+1:τγ
   Cloc = Cloc + Aloc·Bloc(a,:)           # слагаемое τ из ф.(4)
end
```
По структуре Алг. 5 ≈ Алг. 2 (тот же приём с τ). **Когда применять:** при `n>m` (столбцов A много — выгоднее гонять по кольцу A). Естественен при **столбцовом** хранении матриц (фортран).

## 26. Строчно-ориентированные алгоритмы умножения матриц на кольце

**Тезис.** Двойственно q25: распределение **по блочным строкам**, задача µ считает блочную строку `Dµ=Cµ+Σ_{i=1..p} Aµi·Bi` (ф. 5) — линейная комбинация блочных строк B. По кольцу циркулируют **блочные строки матрицы B** [П, §1.2, Алг. 6].

```
Алгоритм 6
Иниц.{row=(µ-1)α+1:µα; row2=(µ-1)γ+1:µγ; Aloc=A(row,:); Bloc=B(row2,:); Cloc=C(row,:); left,right}
for i=1:p
   send(Bloc,right);  recv(Bloc,left)     # строки B идут по кольцу
   τ=µ-i; if τ≤0 then τ=τ+p end
   a=(τ-1)γ+1:τγ
   Cloc = Cloc + Aloc(:,a)·Bloc           # слагаемое τ из ф.(5)
end
```
**Когда применять:** при `n<m` (строк B много — выгоднее гонять B). Естественен при **строчном** хранении (Си). Вывод [П]: соотношение размеров матриц определяет и выбор алгоритма (5 или 6), и язык программирования.

## 27. Систолический алгоритм умножения матриц на торе

**Тезис.** `D=C+AB` на **торе** `N×N` (N=√p) через блочное скалярное произведение `D_{µλ}=C_{µλ}+Σ_{k=1..N} A_{µk}·B_{kλ}` (ф. 6). Блоки A сдвигаются по строкам тора (на запад), блоки B — по столбцам (на север); за N шагов «сдвиг–умножение» каждый узел накапливает свой `D_{µλ}` [П, §1.3, Алг. 9]. (Это классический алгоритм Кэннона; в [П] термин не используется.)

**Начальное выравнивание (Алг. 8).** Чтобы на узле (µ,λ) встретились нужные блоки, до счёта блочную строку µ матрицы A циклически сдвигают **влево на µ−1**, блочный столбец λ матрицы B — **вверх на λ−1**. Тогда у задачи (µ,λ) изначально лежат `A_{µρ}` и `B_{ρλ}`, где `ρ=<λ+µ−1>_N` (остаток от деления λ+µ−1 на N, при нуле ρ=N).

**Систолическое умножение (Алг. 9):**
```
Иниц.{Cloc=C(...); Aloc=A_{µρ}; Bloc=B_{ρλ}; east,west,north,south}
for k=1:N
   send(Aloc,west);  send(Bloc,north)     # отправка
   recv(Aloc,east);  recv(Bloc,south)     # приём
   Cloc = Cloc + Aloc·Bloc                # слагаемое из ф.(6)
end
```
Соседи: west=(µ,λ−1), east=(µ,λ+1), north=(µ−1,λ), south=(µ+1,λ) (с замыканием краёв тора).

**Кольцо vs тор [П]:** на кольце `p` пар приём-отправка и пересылается **целая** матрица A или B; на торе `2√p` пар и пересылаются лишь блочные строка/столбец. При равной арифметике **тор предпочтительнее** — меньше коммуникаций и памяти на узел.

## 28. Параллельный алгоритм разложения Холецкого на кольце

**Тезис.** `A=GGᵀ` на кольце на базе **gaxpy-варианта**: j-й столбец `G(j:n,j)` находится из `v(j:n)=A(j:n,j)−G(j:n,1:j−1)·Gᵀ(1:j−1,j)`, затем делением на `√v(j)`. Готовые столбцы G **передаются по кольцу «эстафетой»**; распределение столбцов — **циклическое** (балансировка) [П, §2.1].

**Простой случай p=n (Алг. 10).** Задача µ хранит столбец `A(:,µ)` и вычисляет `G(µ:n,µ)`. Фактически это **линия**, а не кольцо: первая задача отправляет свой готовый столбец, далее он «по эстафете» проходит все задачи, каждая добавляет одно слагаемое gaxpy (ф. 7). Алгоритм **не систолический** (приём раньше отправки):
```
for j=1:µ-1
   recv(g(j:n),left)
   if µ<n then send(g(j:n),right) end
   Aloc(µ:n) = Aloc(µ:n) - g(µ:n)·g(µ)       # слагаемое из ф.(7)
end
Aloc(µ:n) = Aloc(µ:n)/√(Aloc(µ))             # шаг 2: столбец G(µ:n,µ)
if µ<n then send(Aloc(µ:n),right) end
```
Недостатки: простои (задачи с малыми номерами заканчивают рано, с большими — ждут данных), несбалансированность, сопоставимость времени счёта и обмена.

**Общий случай n/p>1 (Алг. 11).** Каждой задаче — **циклический** набор столбцов `col=µ:p:n` (а не линейный — иначе простои). Топология — **кольцо**: каждый найденный столбец G (кроме последних p−1) делает полный оборот. Локальный индекс `k` ↔ глобальный `j=col(k)`. Это даёт хорошую балансировку и малые простои.

**Связь с [В]:** в векторном алгоритме одна итерация = одна полная операция gaxpy; в параллельном одна итерация задачи = одно слагаемое линейной комбинации (часть gaxpy).

## 29. Параллельный алгоритм LU-разложения на кольце

**Тезис.** `A=LU` на кольце устроен как Холецкий (Алг. 11), но на базе **модификации матрицы внешним произведением** (мм.): по кольцу циркулирует **вектор множителей Гаусса** — k-й столбец L; принявшие его задачи модифицируют свои столбцы блока `A(k+1:n,k+1:n)`. Распределение столбцов — **циклическое** [П, §2.2, Алг. 12].

Два шага векторного LU (в цикле k=1..n−1): (1) `A(k+1:n,k)=A(k+1:n,k)/A(k,k)` — столбец L; (2) `A(k+1:n,k+1:n) −= A(k+1:n,k)·A(k,k+1:n)` — мм. L формируется ниже диагонали, U — выше.
```
Алгоритм 12 (схема)
Иниц.{col=µ:p:n; L=length(col); Aloc=A(:,col); left,right}
# задача-владелец столбца k: вычисляет L(:,k), шлёт вправо, модифицирует свои столбцы
# прочие: recv множителей слева, при необходимости send вправо, модифицируют свои столбцы
```
**Отличия от Холецкого (Алг. 11):** (1) операция — **мм.**, а не gaxpy; (2) модифицируется правый нижний **квадратный блок**, а не нижний треугольник; (3) элементы U формируются «попутно» — `U(k+1,·)` появляется при последней мм. над этой строкой; (4) последний столбец L известен всегда (L(n,n)=1) — для него ни счёта, ни обмена.

**FIFO-гарантия:** столбцы L отправляются в порядке возрастания глобального индекса и не обгоняют друг друга на кольце → `recv(g(k+1:n))` принимает именно k-й столбец.

> **Замечание.** В [П] базовый Алг. 12 дан **без выбора ведущего элемента**. На практике для устойчивости добавляют частичный pivoting (рассылку индексов перестановки строк) — но это усложнение вне базового алгоритма курса.
>
> **Бонус [П]:** есть и алгоритмы на **процессорной решётке** — LU (Алг. 13) и Холецкого (Алг. 14): задача (i,j) хранит `A(i,j)`, множители Гаусса идут с запада на восток, строка U — с севера на юг; элемент участвует в `min{i,j}−1` модификациях.

## 30. Параллельный алгоритм метода прогонки

**Тезис.** Прогонка (метод Томаса) для трёхдиагональной СЛАУ последовательна (прямой и обратный ходы — линейные рекурсии). Лектор разбирает **метод встречных прогонок**: систему делят пополам, в задаче 1 — правая прогонка для первой половины, в задаче 2 — левая для второй; в середине задачи обмениваются прогоночными коэффициентами [П, §3.2–3.3].

**Последовательная прогонка (O(n)):** прямой ход даёт прогоночные коэффициенты `αᵢ,βᵢ` (`xᵢ=αᵢxᵢ₊₁+βᵢ`), обратный — сами `xᵢ`. Обе рекурсии — частный случай вопроса 22 (сводимы к произведению 2×2 матриц → сдваивание).

**Встречные прогонки (2 задачи) [П]:**
1. **Шаг 1.** Параллельно: задача 1 — прямой ход правой прогонки (уравнения 1-й половины), задача 2 — прямой ход левой прогонки (2-я половина).
2. **Шаг 2.** **Единственная коммуникация:** соседние «срединные» уравнения принадлежат разным задачам — они обмениваются парой прогоночных коэффициентов и находят два срединных неизвестных.
3. **Шаг 3.** Параллельно: обратный ход (правая прогонка в 1-й задаче, левая — во 2-й).

**Характеристики (важный разбор [П]):** одна коммуникация, её длительность `tк` **не зависит** от n, а `tа=tа(n)`. По модели сетевой деградации:
```
S = 2 / (1 + tк / tа(n))
```
- при малых n (`tк>tа`) **S<1** — параллельно медленнее (виден провал на графике);
- с ростом n `S→2`.
**Аномалия S>2:** на двухъядерной ЭВМ при `n1<n<n2` ускорение достигало ≈2,4 (>p=2!) — потому что данные параллельной задачи (вдвое меньше памяти) ещё помещались в кэш, а последовательной — уже нет. При `n≥n2` кэш переполняется в обоих случаях и снова `S→2` (закон Амдала).

**Альтернатива (вне [П]):** циклическая редукция — за `~2·log₂n` шагов исключают нечётные неизвестные; ускорение порядка `n/log₂n`, но при росте числа арифметических операций.

---

# ХАРАКТЕРИСТИКИ ПАРАЛЛЕЛЬНЫХ АЛГОРИТМОВ

> Изложение по пособию [П], глава 3. Определения и формулы — лектора (в частности, «масштабируемость через память» и «коэффициент сетевой деградации»).

## 36. Ускорение, эффективность, масштабируемость, закон Амдала

**Тезис.** Три характеристики алгоритма: **ускорение** `S=T1/Tp`, **эффективность** `E=S/p`, **масштабируемость** `M=Vp/V1` (при `Tp=T1`). Верхнюю границу ускорения при фиксированной задаче даёт **закон Амдала** [П, §3.1–3.2].

**Ускорение** (T1 — время лучшего **последовательного** алгоритма того же метода, Tp — параллельного на p задачах):
```
S = T1 / Tp
```
Показывает, во сколько раз параллельный алгоритм быстрее. (Замечание [П]: «скорости вычислений» в науке нет — есть время и ускорение.)

**Эффективность:**
```
E = S/p = T1/(p·Tp)   ∈ (0,1]
```
Показывает, насколько независимо (параллельно) идут расчёты и насколько полно используется аппаратура. Идеал `E=1` (линейное ускорение `S=p`).

**Масштабируемость — определение [П] (через память!):**
```
M = Vp / V1   при условии   Tp = T1
```
где `Vp,V1` — объём памяти, занимаемый параллельным и последовательным процессами. Смысл: хорошо масштабируем тот алгоритм, для которого, чтобы за **то же время** решить **более объёмную** задачу, достаточно добавить исполнительных устройств. Объём расчётов меряют памятью (а не числом операций), т.к. память измеряется проще, быстрее и точнее, а число операций для конкретного алгоритма через неё однозначно выражается.

> ⚠ Это определение лектора отличается от общепринятого (сильное/слабое масштабирование, закон Густафсона `S=p−f(p−1)`). **На экзамене у этого лектора используйте `M=Vp/V1`**; Густафсона — лишь как «есть ещё такой взгляд».

**Закон Амдала.** Пусть `f` — доля операций последовательного алгоритма, которые **нельзя** распараллелить (зависимые информационно/логически/конкуренционно). Тогда `Tp = f·T1 + (1−f)·T1/p`, откуда:
```
        1
S ≤ ───────────── ,     при p→∞:   S ≤ 1/f
     f + (1−f)/p
```
Неравенство (а не равенство) — из-за неучтённых коммуникаций. Частные случаи [П]:
- `f=0,2` ⟹ `S≤5` при любом p (лишние процессоры простаивают/дублируют);
- `f=0` ⟹ `S≤p` (идеал).

Барьер `1/f` нельзя «пробить» искусством программирования — только сменой алгоритма/метода на изначально более параллельный.

**Аномалия S>p [П]:** на методе встречных прогонок (p=2) при средних n ускорение достигало ≈2,4 из-за эффекта кэша (см. q30) — формально нарушая `S≤p`, но это объясняется коммуникациями между уровнями памяти, не учтёнными в законе Амдала.

## 37. Учёт коммуникаций при оценке эффективности. Коэффициент сетевой деградации

**Тезис.** Для сбалансированного алгоритма `Tp = tа + tк` (время арифметики + время коммуникаций, `tа=T1/p`). Отсюда [П, §3.3]:
```
        p·tа        p              1
S = ───────── = ───────── ,   E = ─────── ,   w = tк/tа
     tа + tк      1 + w           1 + w
```
где `w` — **коэффициент сетевой деградации**, `p·tа=T1`.

- `w=0` (зависимостей и обменов нет) ⟹ `E=1` (100%);
- `w≈1` ⟹ `E≈0,5`;
- `w≫1` (сеть «съедает» счёт) ⟹ эффективность деградирует.

**Разложение коэффициента [П] (фирменная деталь курса):**
```
w = tк/tа = (q/Q)·(τк/τа)
```
- `q/Q` — **алгоритмическая часть** (q — число коммуникаций, Q — число арифметических операций; определяется самим алгоритмом);
- `τк/τа` — **техническая часть** (τк — время одного обмена, τа — одной арифметической операции; зависит от конкретной системы).

При этом `τк` складывается из латентности (подготовки) и числа пакетов × время пакета; зависит от маршрутизации и часто определяется **только экспериментально**.

**Пример (встречные прогонки):** одна коммуникация, `tк` не зависит от n, `tа=tа(n)`:
```
S = 2 / (1 + tк/tа(n))
```
при малых n `S<1`, с ростом n `S→2` (см. q30).

**Ограничения модели [П]:** (1) не учтены коммуникации **внутри** ветви (между уровнями памяти ЭВМ); (2) часть обменов идёт **одновременно** со счётом, так что `Tp=tа+tк` — идеализация. Более точные модели (например, **темпоральные сети Петри**) строят уже для реализации на конкретной системе. Существует и обобщающая формула из [6]: `S=T1/((f+α1k+α2p)T1+td)`, из которой при частных значениях параметров получаются и закон Амдала, и модель (20). **Вывод [П]: достоверные характеристики даёт только натурный эксперимент.**

## 38. Правила постановки вычислительных экспериментов и анализа их результатов

**Тезис.** Теоретические оценки приблизительны → окончательный критерий — **натурный эксперимент**. [П, §3.4] предлагает **9-этапную методику** его постановки и анализа.

**Различение [П]:** эксперимент **вычислительный** (величины *рассчитываются*) и **натурный** (*измеряются*); один опыт может быть обоими. Для оценки времени расчётов эксперимент — натурный.

**Девять этапов:**

1. **Постановка цели.** Зачем эксперимент, какую проблему решает, что делали другие.
2. **Выбор инструментальных средств** — программные (реализация/библиотека; язык под алгоритм: фортран — столбцовое хранение и векторная нотация, Си — строчное), системные (ОС и компилятор; Linux даёт более стабильные времена, чем Windows; компилятор должен соответствовать ОС и использовать нужные архитектурные возможности — SSE и др.), аппаратные (нужные ядра/GPU обязаны присутствовать — нельзя изучать параллелизм на одном ядре).
3. **Выбор параметров** — размерности матриц/блоков, точность чисел (одинарная/двойная), число задач. Часть варьируется с обоснованным шагом. Размерности не слишком малы (чтобы шум системы не искажал замер) и не слишком велики (время/стоимость доступа). Для масштабируемости n подбирают так, чтобы `Tp=T1` (по ф. 18).
4. **Теоретическое предсказание результата** методами §3.2–3.3 (Амдал, сетевая деградация) — пусть и оценочно.
5. **Проведение** — вести **лабораторный журнал**: фиксировать этапы 1–4 и все результаты со всеми обстоятельствами. Сохранять выдержку, не повторять бессмысленно одно и то же.
6. **Представление результатов** — двумя способами: **табличный** (полнота) и **графический** (наглядность). Данные хранить максимально подробно — для перестроения графиков при анализе.
7. **Описание** — словами зафиксировать, **как** меняются величины (например: «от n1 до n2 ускорение растёт логарифмически до Smax, затем линейно падает до Smin»). Не «сглаживать» неудобные особенности — в них может быть ключ.
8. **Анализ** — выяснить, **почему** так. Главный инструмент — сопоставление теории (этап 4) с экспериментом. При расхождении вернуться к этапам 1–4. Три исхода: **(а)** теория и опыт совпали (модель адекватна, данные верифицированы); **(б)** построена новая/уточнённая модель (сильный результат, напр. появление модели CUDA); **(в)** согласовать не удалось → **феноменологический подход** (честно описать «как есть»; тогда особо ценен этап 7).
9. **Заключение** — выводы привязать к цели (достигнута / изменена и достигнута / признана неуместной с аргументацией) и к результатам этапа 8. Хороший эксперимент порождает новые, более глубокие вопросы — это признак успеха.

---

# РЕЗЮМЕ ПО ТЕМЕ

## Ключевые понятия (сквозные нити курса)

**Триада «алгоритм — процесс — архитектура» (по Марчуку):** алгоритм *определяет* процесс, протекающий на архитектуре; хороший алгоритм учитывает архитектуру уже на стадии разработки [М].

**Две оси сложности:** арифметическая (флопы) и **коммуникационная** (обмены между уровнями памяти ИЛИ между узлами). Почти всегда выигрывает алгоритм, минимизирующий коммуникации (блочные алгоритмы, метод пирамид, тор вместо кольца).

**Иерархия векторных операций [В]:** saxpy (лучшая) → gaxpy и внешнее произведение (gaxpy вдвое дешевле по обменам) → умножение матриц (основа блочности). Порядок циклов (ijk…kji) задаёт векторную и матричную операции и доступ к памяти.

**Зависимости определяют параллелизм:** информационная (RAW), логическая (управление), конкуренционная (WAW) → граф зависимостей → ЯПФ → задачи.

**Покрытие пространства итераций:** параллелепипеды (разбиение, оптимизация, коммуникации) ↔ пирамиды (дублирование, без коммуникаций) ↔ синтетический метод (параметр — высота пирамиды).

**Параллельные матричные алгоритмы [П]:** систолический шаблон `отправка→приём→арифметика`; кольцо (циркуляция блоков, индекс `τ=µ−j`) ↔ тор (сдвиги A на запад, B на север, выравнивание `ρ=<λ+µ−1>_N`); разложения на кольце — циклическое распределение столбцов, передача «эстафетой».

**Метрики [П]:** `S=T1/Tp`, `E=S/p`, **масштабируемость `M=Vp/V1` при `Tp=T1`**; пределы — Амдал (`S∞=1/f`); сетевая деградация `E=1/(1+w)`, `w=tк/tа=(q/Q)(τк/τа)`.

## Типичные ошибки на экзамене

- **Флинн:** путать конвейер (MISD) с SISD; забывать, что MISD-машин нет, но класс существует.
- **Хокни ≠ замена Флинна:** детализирует только класс MIMD.
- **Сеть vs сеть Петри:** сеть — `(P,T,F)`; сеть Петри — `(P,T,F,M)`. Свойство единственности (п.4) — для **мест**, не переходов.
- **Условие срабатывания** перехода — «**≥**», срабатывание возможно, но не обязательно.
- **Граф зависимостей ≠ стандартный граф.**
- **Функция c** — может быть «не определена» (ожидание данных); на практике это `recv`.
- **Ускорение циклов** `S=∏n_k / Q`: Q — число подмножеств покрытия, не их мощность.
- **Метод пирамид** даёт единственное покрытие (нет оптимизации), обычно не разбиение (дублирование), но без коммуникаций.
- **gaxpy vs внешнее произведение:** одинаковая арифметика (2nm), но обмены различаются вдвое.
- **L в LU — унитреугольная** (диагональ не хранится); в Холецком диагональ G — корни.
- **gaxpy на кольце:** строки → циркулирует x + нужен индекс `τ`; столбцы → циркулируют длинные `ω`, индекс не нужен, но обменов в `n/β` раз больше.
- **Умножение на кольце:** Алг. 5 (столбцы, гоним A) при `n>m`; Алг. 6 (строки, гоним B) при `n<m`.
- **Тор дешевле кольца:** `2√p` пар обменов и лишь блочные строка/столбец vs `p` пар и целая матрица.
- **Холецкий/LU на кольце** — НЕ систолические (приём раньше отправки) и распределение **циклическое** (линейное даёт простои). LU [П] — **без pivoting** в базовом варианте.
- **Масштабируемость** — у этого лектора `M=Vp/V1` при `Tp=T1` (через память!), **не** Густафсон.
- **Сетевая деградация:** `w` делится на алгоритмическую `q/Q` и техническую `τк/τа` части.
- **S>p возможно** (встречные прогонки) из-за кэша — но это вне закона Амдала.
- **Амдал:** `f` — доля **последовательной** части; `S∞=1/f` не зависит от p. Ускорение считать от **лучшего последовательного** алгоритма.

## Советы к подготовке

1. **Учите парами-противопоставлениями:** Флинн↔Хокни, сеть↔сеть Петри, стандартный граф↔граф зависимостей, параллелепипеды↔пирамиды, gaxpy↔внешнее произведение, кольцо↔тор, строки↔столбцы, Холецкий↔LU на кольце, Амдал↔сетевая деградация.
2. **Псевдокод в нотации Голуба** доводите до автоматизма: кольцевой цикл `send(...,right); recv(...,left); τ=µ-j(+p); ...` повторяется в Алг. 2,5,6; торовый `send west/north; recv east/south` — в Алг. 9.
3. **Умейте выводить формулы:** ускорение рекурсии `n/log₂n`, циклов `∏n_k/Q`, Амдал `1/(f+(1−f)/p)`, деградация `E=1/(1+w)`, встречные прогонки `S=2/(1+tк/tа(n))`.
4. **Рисуйте схемы:** пространство итераций со стрелками зависимостей, дерево рассылки, кольцо/тор со стрелками сдвигов, распределение блоков на торе (выравнивание).
5. **Для гл. 3** держите в голове 9 этапов эксперимента и три исхода анализа; знайте «фирменные» определения лектора (`M=Vp/V1`, `w=(q/Q)(τк/τа)`).

## Карта «вопрос → источник»

| № | Тема | Источник |
|---|---|---|
| 1 | Виноград | [В] §1.3 |
| 3–4 | Флинн, Хокни | [М] §1.1.1–1.1.2 |
| 5 | Модели алгоритмов | [М] §1.2 |
| 6–7 | Сети, сети Петри | [М] §1.3.3 |
| 9–11 | Ациклическое распараллеливание | [М] §2.1 |
| 12–14 | Циклическое (пирамиды, параллелепипеды, синтез) | [М] §2.2 |
| 15–20 | Векторные алгоритмы (умножение, СЛАУ, LU, Холецкий) | [В] §1–3 |
| 21–22 | Рассылка по дереву, рекурсии | [М] §1.2.3, §2.2.4 |
| 23–24 | gaxpy на кольце (строки/столбцы) | [П] §1.1, Алг. 1–3 |
| 25–26 | Умножение матриц на кольце (столбцы/строки) | [П] §1.2, Алг. 5–6 |
| 27 | Умножение матриц на торе | [П] §1.3, Алг. 7–9 |
| 28 | Холецкий на кольце | [П] §2.1, Алг. 10–11 |
| 29 | LU на кольце | [П] §2.2, Алг. 12 (+решётка §2.3) |
| 30 | Прогонка (встречные прогонки) | [П] §3.2–3.3 |
| 36 | Ускорение, эффективность, масштабируемость, Амдал | [П] §3.1–3.2 |
| 37 | Коммуникации, коэффициент сетевой деградации | [П] §3.3 |
| 38 | Постановка экспериментов | [П] §3.4 |
